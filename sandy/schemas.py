"""DB-agnostic row types and dataclasses used across Sandy pipelines.

Skeleton module created by task 1.3. The real TypedDict row types for the
raw and derived tables plus the `FeatureVector`, `ModelArtifact`,
`TopFeature`, and `PredictionResult` dataclasses are implemented in task 3.4.
"""
from __future__ import annotations

# TODO(task 3.4): implement TypedDict row types and shared dataclasses.
