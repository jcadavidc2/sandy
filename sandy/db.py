"""Database layer for Sandy.

Skeleton module created by task 1.3. The real SQLAlchemy engine factory,
session context manager, and `bootstrap_schema()` DDL runner are implemented
in task 3.1.
"""
from __future__ import annotations

# TODO(task 3.1): implement create_engine(), session context manager, and
# bootstrap_schema().
