"""Structured JSON logging for Sandy.

Skeleton module created by task 1.3. The real `configure_logging()` and
`get_logger()` helpers that emit one JSON object per line with
`timestamp`/`level`/`component`/`message` fields are implemented in task 2.2.
"""
from __future__ import annotations

# TODO(task 2.2): implement configure_logging() and get_logger().
