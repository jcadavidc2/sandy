"""Configuration loading for Sandy.

Skeleton module created by task 1.3. The real `Config` dataclass hierarchy
(`DatabaseConfig`, `ModelConfig`, `IngestConfig`, `TrainingConfig`,
`LoggingConfig`) and `load_config()` with precedence defaults < TOML < env <
CLI flags are implemented in task 2.1.
"""
from __future__ import annotations

# TODO(task 2.1): implement Config dataclasses and load_config().
